#include <stdio.h>

/**
 * main - Entry point
 * a beautiful code that passes the Betty checks
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("a beautiful code that passes the Betty checks\n");
	return (0);
}
